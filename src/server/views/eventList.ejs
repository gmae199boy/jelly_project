<html>
    <head>
        <%- include('partials/head') %>
    </head>
    <body>
        <%- include('partials/navbar') %>
        <div class="section">
            <div class="header">
                <h1>Event</h1>
                <h2>이벤트 목록 리스트가 있는 페이지</h2>

                <% if(donor == undefined){ %>
                    <div class="text-muted">로그인을 하셔야 글쓰기가 가능합니다.</div>
                <% }else{ %>
                    <a href="/new"><button class="btn btn-info"> 새 아이템 추가하기</button></a>
                <% } %>
            </div>

            <div class="row">
                <% if (events !== null){events.forEach((event)=>{ %>
                <div class="col-lg-4">
                    <a href="/event/<%= event.eventId %>">
                        <div class="card">
                            <div class="card-image"></div>
                            <div class="card-title"><%= event.name %></div>
                            <div class="card-comment"><%= event.desc %> </div>
                        </div>
                    </a>
                </div>
                <%  })} %>
            </div>
        </div>
    </body>
</html>